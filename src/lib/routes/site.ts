/**
 * Placeholder canonical origin for metadata routes. The production domain is
 * a deployment decision that is intentionally deferred; nothing else may
 * depend on this value.
 */
export const SITE_BASE_URL = "https://forward.example";
